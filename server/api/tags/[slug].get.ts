export default defineEventHandler(async event=>{const slug=(getRouterParam(event,'slug')||'').toLowerCase();const tag=await usePrisma().tag.findUnique({where:{slug},select:{id:true,name:true,slug:true}});if(!tag)return apiError(event,404,'TAG_NOT_FOUND','标签不存在');const posts=await usePrisma().post.findMany({where:{status:'PUBLISHED',tags:{some:{tagId:tag.id}}},orderBy:[{publishedAt:'desc'},{id:'desc'}],select:publicPostListSelect});return{data:{tag,items:posts.map(serializePublicPost),total:posts.length}}})
