<script setup lang="ts">
type Project={id:number;name:string;summary:string;status:'ACTIVE'|'ARCHIVED'|'SOLD'|'BUILDING';websiteUrl:string|null;githubUrl:string|null;techStack:string[]}
const {data:response}=await useFetch<{data:Project[]}>('/api/projects');const projects=computed(()=>response.value?.data||[])
const labels={ACTIVE:'已上线',BUILDING:'开发中',SOLD:'已出售',ARCHIVED:'已归档'}
useSeoMeta({title:'项目',description:'个人项目与产品实践'})
</script>
<template><section class="project-page"><header class="hero"><p class="eyebrow">PROJECTS</p><h1>项目</h1><p class="lead">一些正在构建或已经完成的产品与开源实践。</p></header><div class="grid"><article v-for="p in projects" :key="p.id" class="project-card"><a :href="p.websiteUrl||p.githubUrl||'#'" target="_blank" rel="noopener noreferrer"><div class="top"><h2>{{p.name}}</h2><span>{{labels[p.status]}}</span></div><p>{{p.summary}}</p><div class="tech"><span v-for="item in p.techStack" :key="item">{{item}}</span></div></a><a v-if="p.githubUrl" class="github" :href="p.githubUrl" target="_blank" rel="noopener noreferrer">GitHub ↗</a></article><p v-if="!projects.length" class="empty">暂无项目</p></div></section></template>
<style scoped>.project-page{display:grid;gap:30px}.grid{display:grid;gap:16px}.project-card{position:relative;padding:20px;border:1px solid var(--color-border);border-radius:var(--radius-md);background:var(--color-background);transition:.2s}.project-card:hover{border-color:var(--color-primary);transform:translateY(-2px)}.top{display:flex;justify-content:space-between;gap:12px}.top h2{margin:0;font-size:17px}.top span,.tech span{padding:3px 7px;border-radius:99px;background:var(--color-background-secondary);color:var(--color-text-secondary);font-size:10px}.project-card p{margin:12px 0;color:var(--color-text-secondary);font-size:13px;line-height:1.7}.tech{display:flex;flex-wrap:wrap;gap:7px;padding-right:70px}.github{position:absolute;right:20px;bottom:22px;color:var(--color-primary);font-size:11px}.empty{text-align:center}</style>
